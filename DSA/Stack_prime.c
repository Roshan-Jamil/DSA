#include <stdio.h>
#include <stdlib.h>
#define max 5

int top = -1;
int stack [max];

void push(int data)
{
    if(top == max -1)
    {
        printf("stack is constant ");
    }
    else
    {
        
    }
}